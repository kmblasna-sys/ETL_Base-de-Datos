# Configuración de Conexión a la Base de Datos MySQL (Plantilla de ejemplo)
# Copia este archivo a `.env` y rellena con tus credenciales locales.

DB_HOST=localhost
DB_PORT=3306
DB_USER=tu_usuario
DB_PASSWORD=tu_contraseña
DB_NAME=Mineria_BD
